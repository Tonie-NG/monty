#include "monty.h"

/**
 * nop_func - a function that does nothing
 * @head: pointer to the head of a doubly linked list
 * @line_number: current line number of the file
 */
void nop_func(stack_t **head, usint line_number)
{
	(void)head;
	(void)line_number;
}

